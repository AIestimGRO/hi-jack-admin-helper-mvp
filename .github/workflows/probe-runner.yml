name: Probe feature branch runner

on:
  push:
    branches:
      - fix/jackside-shared-ip-start-limit-20260915

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo FEATURE_BRANCH_ACTIONS_OK
